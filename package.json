{
  "name": "tiny-orchestrator-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w tiny-orchestrator run build && npm -w @tiny-orchestrator/openai run build",
    "lint": "npm -w tiny-orchestrator run lint && npm -w @tiny-orchestrator/openai run lint",
    "test": "npm -w tiny-orchestrator run test",
    "test:node": "npm -w tiny-orchestrator run test:node"
  }
}
